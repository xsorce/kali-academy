# Themes

Kali Academy installs only lightweight terminal defaults.

Personal Xfce wallpaper/theme settings are deliberately **not overwritten by updates**.

Future repo assets can live here, for example:

```text
themes/wallpapers/
themes/starship/
themes/xfce-defaults/
```

The installer should copy a theme only when the user explicitly opts into it or no user preference
exists.
